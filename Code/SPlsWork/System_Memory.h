namespace System;
        // class declarations
         class SequencePosition;
         class MemoryExtensions;
    static class MemoryExtensions 
    {
        // class delegates

        // class events

        // class functions
        SIGNED_LONG_INTEGER_FUNCTION GetHashCode ();
        STRING_FUNCTION ToString ();

        // class variables
        INTEGER __class_id__;

        // class properties
    };

namespace System.Runtime.InteropServices;
        // class declarations
         class SequenceMarshal;
         class MemoryMarshal;
    static class SequenceMarshal 
    {
        // class delegates

        // class events

        // class functions
        SIGNED_LONG_INTEGER_FUNCTION GetHashCode ();
        STRING_FUNCTION ToString ();

        // class variables
        INTEGER __class_id__;

        // class properties
    };

    static class MemoryMarshal 
    {
        // class delegates

        // class events

        // class functions
        SIGNED_LONG_INTEGER_FUNCTION GetHashCode ();
        STRING_FUNCTION ToString ();

        // class variables
        INTEGER __class_id__;

        // class properties
    };

namespace System.Buffers;
        // class declarations
         class BuffersExtensions;
         class OperationStatus;
         class StandardFormat;
         class MemoryHandle;
    static class BuffersExtensions 
    {
        // class delegates

        // class events

        // class functions
        SIGNED_LONG_INTEGER_FUNCTION GetHashCode ();
        STRING_FUNCTION ToString ();

        // class variables
        INTEGER __class_id__;

        // class properties
    };

    static class OperationStatus // enum
    {
        static SIGNED_LONG_INTEGER Done;
        static SIGNED_LONG_INTEGER DestinationTooSmall;
        static SIGNED_LONG_INTEGER NeedMoreData;
        static SIGNED_LONG_INTEGER InvalidData;
    };

namespace System.Buffers.Text;
        // class declarations
         class Base64;
         class Utf8Formatter;
         class Utf8Parser;
         class HexCasing;
    static class Base64 
    {
        // class delegates

        // class events

        // class functions
        static SIGNED_LONG_INTEGER_FUNCTION GetMaxDecodedFromUtf8Length ( SIGNED_LONG_INTEGER length );
        static SIGNED_LONG_INTEGER_FUNCTION GetMaxEncodedToUtf8Length ( SIGNED_LONG_INTEGER length );
        SIGNED_LONG_INTEGER_FUNCTION GetHashCode ();
        STRING_FUNCTION ToString ();

        // class variables
        INTEGER __class_id__;

        // class properties
    };

    static class Utf8Formatter 
    {
        // class delegates

        // class events

        // class functions
        SIGNED_LONG_INTEGER_FUNCTION GetHashCode ();
        STRING_FUNCTION ToString ();

        // class variables
        INTEGER __class_id__;

        // class properties
    };

    static class Utf8Parser 
    {
        // class delegates

        // class events

        // class functions
        SIGNED_LONG_INTEGER_FUNCTION GetHashCode ();
        STRING_FUNCTION ToString ();

        // class variables
        INTEGER __class_id__;

        // class properties
    };

    static class HexCasing // enum
    {
        static LONG_INTEGER Uppercase;
        static LONG_INTEGER Lowercase;
    };

namespace System.Buffers.Binary;
        // class declarations
         class BinaryPrimitives;
    static class BinaryPrimitives 
    {
        // class delegates

        // class events

        // class functions
        static SIGNED_INTEGER_FUNCTION ReverseEndianness ( SIGNED_INTEGER value );
        SIGNED_LONG_INTEGER_FUNCTION GetHashCode ();
        STRING_FUNCTION ToString ();

        // class variables
        INTEGER __class_id__;

        // class properties
    };

